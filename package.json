{
  "name": "simplate",
  "description": "Create static webpage simply.",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.24.5",
    "node-sass": "^4.9.0",
    "npm-run-all": "^4.1.3"
  },
  "scripts": {
    "watch": "npm-run-all --parallel watch:*",
    "watch:sass": "node-sass -w sass/main.scss css/main.css",
    "watch:sync": "browser-sync start -ws --index index.html -f css/*.css"
  }
}
